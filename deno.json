{
  // A human-readable name for your project
  "name": "dinodoro",
<<<<<<< HEAD
  "version": "1.0.0",
  "exports": "./dinodoro.ts",
=======
  "version": "1.0.1",
>>>>>>> 7330ac3 (now version 1.0.1)
  "author": "CodeMonkeyWTF",

  // Defines tasks that can be run with `deno task <task_name>`
  // This is a great way to manage permissions and run scripts easily.
  "tasks": {
    "start": "deno run --allow-run dinodoro.ts",
    "dev": "deno run --allow-run --watch dinodoro.ts"
  },

  // Configuration for the Deno linter (`deno lint`)
  "lint": {
    "files": {
      "include": ["dinodoro.ts"],
      "exclude": ["deno.lock"]
    },
    "rules": {
      "tags": ["recommended"],
      "exclude": ["no-explicit-any"]
    }
  },

  // Configuration for the Deno formatter (`deno fmt`)
  "fmt": {
    "files": {
      "include": ["dinodoro.ts"],
      "exclude": ["deno.lock"]
    },
    "options": {
      "useTabs": false,
      "lineWidth": 80,
      "indentWidth": 2,
      "singleQuote": false
    }
  },

  // TypeScript compiler options
  "compilerOptions": {
    "lib": ["deno.ns"]
  }
}

